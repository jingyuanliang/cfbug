apiVersion: apps/v1
kind: Deployment
metadata:
  name: poison
  labels:
    app: poison
spec:
  replicas: 1
  selector:
    matchLabels:
      app: poison
  template:
    metadata:
      labels:
        app: poison
    spec:
      containers:
        - name: agnhost
          image: registry.k8s.io/e2e-test-images/agnhost:2.63.0
          args:
            - netexec
            - --http-override=/hostname
          ports:
            - name: http
              containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: wanted
spec:
  type: ClusterIP
  clusterIP: 34.118.225.20
  selector:
    app: poison
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
